# 第 3 章 校园社交匹配推荐系统需求分析

本章在项目目标和同类方案不足的基础上，对校园社交匹配推荐系统进行需求分析。系统面向校园用户匹配场景，核心任务是根据用户标签、校园属性和反馈行为生成可解释的匹配推荐结果。本章重点分析本文实际完成且工作量较集中的主要功能，包括用户与标签维护、画像构建、推荐生成、推荐解释和反馈更新。

## 3.1 需求分析综述

校园社交匹配推荐系统的主要使用者为校园用户和系统管理员。校园用户维护个人信息与兴趣标签，获取推荐对象，查看推荐依据，并对推荐结果进行反馈。系统管理员负责维护基础用户数据、标签体系和评估材料，保证推荐链路具备稳定的数据来源。外部大语言模型服务仅作为解释文本润色能力参与系统，不参与召回、排序和重排计算。

系统需求可以概括为以下四类。第一，系统应支持用户信息和标签关系维护，为画像生成提供输入。第二，系统应根据用户标签构建兴趣画像，并通过倒排召回、相似度排序和校园规则重排生成 Top-K 推荐结果。第三，系统应给出可追溯的推荐解释，使用户能够理解推荐对象与自身兴趣和校园属性之间的关系。第四，系统应记录用户反馈，并将反馈结果反映到后续画像更新中。

## 3.2 用户与标签维护需求

用户与标签维护是推荐链路的数据入口。校园用户需要维护昵称、年级、专业、学院、个人简介和兴趣标签等信息；系统管理员需要维护标签定义、标签分类和基础用户数据。该功能不直接产生推荐结果，但决定画像构建的输入质量，是后续推荐生成和解释生成的前提。

用户与标签维护功能用例图如图 3-1 所示。

> 图 3-1 用户与标签维护用例图

如图 3-1 所示，校园用户主要完成个人信息维护和兴趣标签维护，系统管理员主要完成标签体系维护和基础用户数据维护。用户标签关系保存后，画像模块即可根据标签类型、标签权重和更新时间计算用户兴趣画像。

表 3-1 用户与标签维护用例说明

| 用例编号 | UC1 |
| --- | --- |
| 用例名称 | 用户与标签维护 |
| 活动者 | 校园用户、系统管理员 |
| 优先级 | 高 |
| 描述 | 校园用户维护个人资料和兴趣标签，系统管理员维护标签定义、标签分类和基础用户数据。 |
| 前置条件 | 用户进入系统，系统已具备基础标签分类。 |
| 基本流程 | （1）用户进入个人信息或标签维护入口；（2）系统展示当前用户资料和已有标签；（3）用户新增、删除或调整标签；（4）系统保存用户标签关系；（5）系统在后续画像构建时读取最新标签关系。 |
| 扩展流程 | 管理员可新增标签分类或调整标签名称，系统在后续维护中使用更新后的标签体系。 |
| 后置条件 | 用户基础信息和标签关系被保存，画像构建具备可计算输入。 |

## 3.3 画像构建与推荐生成需求

画像构建与推荐生成是系统的核心功能。系统应根据用户标签关系生成兴趣画像，并依据画像完成候选召回、基础排序和校园规则重排。该功能需要解决三个问题：一是把离散标签转换为可计算的权重向量；二是避免对全量用户逐一计算相似度；三是让推荐结果符合校园学习、社团和兴趣匹配场景。

画像构建与推荐生成用例图如图 3-2 所示。

> 图 3-2 画像构建与推荐生成用例图

如图 3-2 所示，校园用户发起推荐请求后，系统首先获取或重建用户画像，然后根据画像中的 Top-K 标签召回候选用户，再依次完成相似度排序和校园规则重排。系统管理员可重建画像和倒排索引，以保证数据变更后推荐链路能够重新计算。

表 3-2 画像构建与推荐生成用例说明

| 用例编号 | UC2 |
| --- | --- |
| 用例名称 | 画像构建与推荐生成 |
| 活动者 | 校园用户、系统管理员 |
| 优先级 | 高 |
| 描述 | 系统根据用户标签生成画像，并通过召回、排序和重排生成 Top-K 推荐结果。 |
| 前置条件 | 用户已维护基础信息和标签，系统已具备候选用户与标签关系。 |
| 基本流程 | （1）用户请求推荐结果；（2）系统读取或构建目标用户画像；（3）系统根据 Top-K 标签从倒排索引召回候选用户；（4）系统计算目标用户与候选用户的相似度；（5）系统依据年级、专业、社团等校园规则进行重排；（6）系统返回 Top-K 推荐结果。 |
| 扩展流程 | 当画像或倒排索引缺失时，管理员可触发重建；系统也可根据 MySQL 中的用户标签关系重新生成画像和索引。 |
| 后置条件 | 系统生成推荐结果，并保留排序分数、规则命中和追踪标识。 |

## 3.4 推荐解释需求

推荐解释用于说明推荐结果产生的依据。系统不应只返回推荐对象列表，还应说明候选用户与目标用户之间的共同标签、标签贡献、校园规则命中和可信连接原因。解释内容必须来自推荐计算过程中的证据，不能脱离排序和重排结果单独生成。

推荐解释功能用例图如图 3-3 所示。

> 图 3-3 推荐解释用例图

如图 3-3 所示，校园用户查看推荐对象时，可以进一步查看推荐解释。系统先从推荐结果中读取标签贡献和规则命中，再构造解释证据；当外部大语言模型服务可用时，系统可对规则解释文本进行润色；当外部服务不可用时，系统返回规则解释文本。

表 3-3 推荐解释用例说明

| 用例编号 | UC3 |
| --- | --- |
| 用例名称 | 查看推荐解释 |
| 活动者 | 校园用户、外部大语言模型服务 |
| 优先级 | 高 |
| 描述 | 用户查看某条推荐结果的推荐依据，系统返回可追溯解释文本和证据字段。 |
| 前置条件 | 系统已生成推荐结果，并保存推荐解释所需的标签贡献和规则命中数据。 |
| 基本流程 | （1）用户点击某条推荐结果的解释入口；（2）系统读取推荐结果及其证据；（3）系统抽取共同标签、规则命中和可信连接原因；（4）系统生成解释文本；（5）系统向用户展示推荐依据。 |
| 扩展流程 | 若外部大语言模型服务可用，系统对解释文本进行润色；若服务不可用，系统返回规则解释文本。 |
| 后置条件 | 用户能够查看推荐依据，系统保留解释文本和证据数据。 |

## 3.5 用户反馈与画像更新需求

用户反馈用于让推荐结果影响后续画像。系统应记录用户对推荐对象的关注或忽略行为，并将反馈与对应推荐结果关联。反馈更新不是重新设计完整推荐模型，而是在已有画像基础上对相关标签权重进行轻量调整，使后续推荐能够体现用户行为变化。

用户反馈与画像更新用例图如图 3-4 所示。

> 图 3-4 用户反馈与画像更新用例图

如图 3-4 所示，校园用户对推荐对象提交反馈后，系统保存反馈记录，并读取推荐结果中的证据标签。若用户选择关注，系统提高相关标签权重；若用户选择忽略，系统降低相关标签权重。随后系统重建用户画像，使后续推荐使用更新后的画像。

表 3-4 用户反馈与画像更新用例说明

| 用例编号 | UC4 |
| --- | --- |
| 用例名称 | 用户反馈与画像更新 |
| 活动者 | 校园用户 |
| 优先级 | 高 |
| 描述 | 用户对推荐对象提交关注或忽略反馈，系统记录反馈并更新用户画像。 |
| 前置条件 | 用户已获得推荐结果，推荐结果中保存了推荐对象和证据标签。 |
| 基本流程 | （1）用户对推荐对象点击关注或忽略；（2）系统保存反馈类型、推荐对象和反馈时间；（3）系统读取该推荐结果中的主要证据标签；（4）系统根据反馈类型调整相关标签权重；（5）系统重建用户画像；（6）后续推荐使用更新后的画像。 |
| 扩展流程 | 若推荐结果缺少可用证据标签，系统仅保存反馈记录，不执行画像权重调整。 |
| 后置条件 | 用户反馈被保存，画像版本更新，后续推荐能够体现反馈影响。 |

## 3.6 非功能需求

除功能需求外，系统还需满足可维护性、可解释性、可验证性和响应可用性等非功能需求。可维护性要求系统模块边界清晰，推荐编排、画像、召回、排序、重排、解释和反馈模块能够独立理解和测试。可解释性要求推荐结果保留标签贡献、规则命中和可信连接原因，便于用户理解推荐依据。可验证性要求系统具备自动化测试、治理检查和离线评估材料，保证论文中的分析能够回到系统事实。响应可用性要求系统在小规模校园用户数据下保持可接受的接口响应速度。

表 3-5 非功能需求说明

| 非功能需求 | 需求说明 | 对系统设计的影响 |
| --- | --- | --- |
| 可维护性 | 模块职责清晰，核心链路便于扩展 | 按 Controller、Service、Strategy、Mapper、Repository 分层 |
| 可解释性 | 推荐结果应具备可追溯依据 | 保存标签贡献、规则命中、可信连接原因和解释证据 |
| 可验证性 | 系统功能和推荐逻辑应可重复验证 | 建立自动化测试、治理检查和离线评估材料 |
| 响应可用性 | 推荐接口在校园用户数据规模下保持可用响应 | 使用倒排召回、画像缓存和推荐缓存降低重复计算 |

## 3.7 本章小结

本章围绕校园用户匹配推荐系统的重点功能进行了需求分析。系统核心需求集中在用户与标签维护、画像构建与推荐生成、推荐解释、用户反馈与画像更新四个方面。上述需求明确了第 4 章概要设计的模块边界，也为第 5 章详细设计与实现中的类图、时序图和界面展示提供依据。
