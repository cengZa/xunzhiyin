# Task Intake

`07_task/` 保留历史任务输入，但从现在开始不再作为唯一执行载体。

## 使用规则
- 新任务可以先落在这里作为输入说明。
- 一旦任务进入实施阶段，应在 `docs/exec-plans/active/` 建立对应计划。
- 已完成任务的计划应转入 `docs/exec-plans/completed/`。

## 当前挂载
- `task_01_init_project.md` -> 待迁移为正式执行计划
